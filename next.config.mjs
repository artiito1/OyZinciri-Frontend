/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['gateway.pinata.cloud','pink-hilarious-beaver-575.mypinata.cloud','api.pinata.cloud'],
  },
};

export default nextConfig;
